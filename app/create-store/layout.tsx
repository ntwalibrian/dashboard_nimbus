export const metadata = {
  title: "Create Store | Nimbus",
  description: "Create your Nimbus e-commerce store",
};

export default function CreateStoreLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
